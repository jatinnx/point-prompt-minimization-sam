# Runtime for the DLRSD point-prompt harness.
#
# These are the versions resolved in /home/cse-sdpl/pytorch-env on this machine,
# pinned so a rerun reproduces the numbers in artifacts/. torch is listed for the
# record but was already present -- reinstalling it into a fresh venv pulls ~4 GB
# on top of SAM 3's 3.3 GB, so install the rest into that environment instead:
#
#   /home/cse-sdpl/pytorch-env/bin/pip install -r requirements.txt
#
numpy==2.5.2
opencv-python==5.0.0.93
pillow==12.3.0
scipy==1.18.1
matplotlib==3.11.1
pytest==8.4.2

# torch==2.13.0+cu130   # pre-existing; CUDA 13.0 wheel, from the PyTorch index

# SAM 3 -- the only model here. The weights are not a pip dependency: they sit in
# the project at SAM-modals/sam3 (3.3 GB, gitignored), copied out of the Hugging
# Face cache with the blob symlinks dereferenced, and both branches load from
# there. Reproduce that copy on a fresh machine with
#
#   hf download facebook/sam3 --local-dir SAM-modals/sam3
#
# which needs `hf auth login` because the repo is gated. Sam3Model and
# Sam3TrackerModel need transformers 5.x; 5.16.1 produced everything in artifacts/.
transformers==5.16.1
